import { h, FunctionalComponent } from '@stencil/core';
import { App } from '../app';

export const Examples: FunctionalComponent<{ component: App }> = ({ component }) => {
    return (
        <aside role="complementary" class="col-md-5 col-lg-5 px-4">

        </aside>
    );
}
